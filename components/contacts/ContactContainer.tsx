import React from 'react'
import styled from 'styled-components'

import { Children } from 'app/shared/types'

export const ContactContainer = ({ children }: Children): JSX.Element => {
	return <>{children}</>
}
