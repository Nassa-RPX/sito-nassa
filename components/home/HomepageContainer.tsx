import React from 'react'
import styled from 'styled-components'

import { Children } from 'app/shared/types'

export const HomepageContainer = ({ children }: Children): JSX.Element => {
	return <>{children}</>
}
